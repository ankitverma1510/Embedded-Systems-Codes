In this program uart driver is done. For this the corresponding uart port is disabled first before writing the values of baud_rate and other settings. Then it is enabled. In this program all uart ports are written. Based on the argument passed in uart_init function, corresponding uart will be initialized. Then by using uart_write and uart_read functions we can write or read data from the uart pins.
